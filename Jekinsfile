pipeline {
agent any

```
stages {
    stage('Checkout Code') {
        steps {
            git branch: 'main',
                url: 'https://github.com/your-username/gym-website.git',
                credentialsId: 'github-credentials'
        }
    }

    stage('Build') {
        steps {
            echo 'No build required for static HTML site'
        }
    }

    stage('Test') {
        steps {
            echo 'No tests configured'
        }
    }

    stage('Deploy') {
        steps {
            echo 'Deploying website...'

            // Example: copy files to Apache server directory
            sh '''
            sudo rm -rf /var/www/html/*
            sudo cp -r * /var/www/html/
            '''
        }
    }
}
```

}
